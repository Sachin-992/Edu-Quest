import React from 'react';

interface ThemeToggleProps {
    className?: string;
}

// Dark mode disabled - only light mode available
const ThemeToggle: React.FC<ThemeToggleProps> = () => {
    return null;
};

export default ThemeToggle;
